import { RowProps } from './row';

// Packages
import styled from 'styled-components';
import { Row as RowAntDesign } from 'antd/es/grid';

export const RowContainer = styled(RowAntDesign)<RowProps>``;
